import React, { FC } from 'react';
import Lobby from '../../pages/Lobby/Lobby';
import './App.scss';

const App: FC = () => {
  return (
    <div className="App test">
      <Lobby />
    </div>
  );
};

export default App;
